// Package postgres is the durable-state storage adapter: spaces, channels,
// members, roles, messages. Supports both embedded and networked Postgres so
// the single-binary and distributed deployment modes share one schema.
package postgres
