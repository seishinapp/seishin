// Package native implements the CXP/1 gateway: QUIC/WebTransport handshake,
// control-frame framing, and voice/pose/probe datagram handling. It is an
// ordinary Canonical Platform API client with an elevated capability set —
// nothing here is unreachable by a third-party bot with the same
// capabilities. See docs/protocol.md.
package native
